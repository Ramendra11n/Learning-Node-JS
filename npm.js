//npm is a library that can be used to import the code 
// a package/a dependency /a module is a some code 
 /*there arw two ways to use the library either to install the package globally or to install the package locally 
 we can install the package locally(package will run on  a single program)
npm i <package name>

 globally (running package across all program )
 npm install -g <package name > ;
 */

 /*
 package.json :stores the important info about the files 

 manual :create the .json file and then create every feild 
 npm init (enter the data step by step);
 npm init-y(initialise everything with the default value);


 */ 
